import { instagramEntryType } from "./instagramEntry";
import { projectType } from "./project";
import { siteSettingsType } from "./siteSettings";

export const schemaTypes = [projectType, instagramEntryType, siteSettingsType];
